# وبسایت آکادمی 84

وبسایت رسمی آکادمی 84 - مرکز تخصصی آموزش تصویر و رسانه. طراحی شده با Next.js 14 و React.

## ویژگی‌ها

- ✨ طراحی مدرن، مینیمال و حرفه‌ای
- 🎨 استفاده از Tailwind CSS با پالت رنگی مینیمال
- 📱 کاملاً Responsive - سازگار با موبایل، تبلت و دسکتاپ
- ⚡ عملکرد سریع با Next.js 14 App Router
- 🔍 بهینه‌سازی شده برای SEO
- 🌐 پشتیبانی کامل از زبان فارسی (RTL)
- 🎬 انیمیشن‌های نرم و حرفه‌ای (Count-up، Scroll animations)
- 💫 Micro-interactions پیشرفته
- ⭐ سکشن نظرات دانشجویان (Testimonials)
- 📊 آمار پویا با Count-up animation

## بخش‌های سایت

### بخش آکادمی
- صفحه اصلی آکادمی
- لیست دوره‌ها و جزئیات دوره
- معرفی اساتید
- گالری دانشجویان و نمونه کارها
- فرم ثبت‌نام آنلاین

### بخش شرکت
- صفحه اصلی شرکت
- لیست خدمات شرکت

### بخش‌های عمومی
- صفحه اصلی (Home)
- درباره ما
- وبلاگ با پست‌های مختلف
- تماس با ما با فرم تماس

## پیش‌نیازها

برای اجرای این پروژه، باید موارد زیر را نصب کنید:

- Node.js 18.0 یا بالاتر
- npm یا yarn

## نصب و راه‌اندازی

1. کلون کردن یا دانلود پروژه:
```bash
cd 84hashtad
```

2. نصب وابستگی‌ها:
```bash
npm install
# یا
yarn install
```

3. اجرای پروژه در حالت توسعه:
```bash
npm run dev
# یا
yarn dev
```

4. باز کردن مرورگر و رفتن به:
```
http://localhost:3000
```

## ساخت برای تولید

برای ساخت نسخه تولیدی:

```bash
npm run build
# یا
yarn build
```

سپس برای اجرای نسخه production:

```bash
npm start
# یا
yarn start
```

## ساختار پروژه

```
84hashtad/
├── app/                    # صفحات Next.js (App Router)
│   ├── academy/           # صفحات بخش آکادمی
│   ├── blog/              # صفحات وبلاگ
│   ├── about/             # صفحه درباره ما
│   ├── contact/           # صفحه تماس
│   ├── layout.tsx         # Layout اصلی
│   ├── page.tsx           # صفحه اصلی
│   └── globals.css        # استایل‌های عمومی و انیمیشن‌ها
├── components/            # کامپوننت‌های React
│   ├── Header.tsx         # هدر با CTA button
│   ├── Hero.tsx          # Hero section با Count-up
│   ├── CourseCard.tsx    # کارت دوره
│   ├── ServiceCard.tsx   # کارت خدمات
│   ├── TestimonialCard.tsx # کارت نظرات
│   ├── CountUp.tsx       # کامپوننت Count-up animation
│   └── AnimatedSection.tsx # کامپوننت برای scroll animations
├── hooks/                 # Custom hooks
│   └── useScrollAnimation.ts # Hook برای scroll animations
├── lib/                   # توابع و داده‌های کمکی
│   └── data/             # داده‌های نمونه
│       ├── courses.ts
│       ├── services.ts
│       ├── teachers.ts
│       ├── students.ts
│       ├── posts.ts
│       └── testimonials.ts # نظرات دانشجویان
├── public/               # فایل‌های استاتیک
│   └── images/
│       ├── logos/        # لوگوها
│       └── testimonials/ # تصاویر نظرات
└── package.json          # وابستگی‌ها و اسکریپت‌ها
```

## تنظیمات

### متغیرهای محیطی

یک فایل `.env.local` در ریشه پروژه ایجاد کنید (می‌توانید از `.env.example` استفاده کنید):

```env
# مثال برای متغیرهای محیطی (در صورت نیاز)
# DATABASE_URL=your_database_url
# API_KEY=your_api_key
```

## دیپلوی

### دیپلوی روی Vercel (پیشنهادی)

1. پروژه را در GitHub/GitLab/Bitbucket آپلود کنید
2. به [Vercel](https://vercel.com) بروید و وارد شوید
3. روی "Add New Project" کلیک کنید
4. ریپازیتوری خود را انتخاب کنید
5. تنظیمات را تایید کنید (Vercel به طور خودکار Next.js را تشخیص می‌دهد)
6. روی "Deploy" کلیک کنید

### دیپلوی روی سایر پلتفرم‌ها

این پروژه را می‌توانید روی هر پلتفرمی که از Next.js پشتیبانی می‌کند دیپلوی کنید:
- Netlify
- Digital Ocean
- AWS
- سرور شخصی با Node.js

## سفارشی‌سازی

### تغییر محتوا

برای تغییر محتوا، فایل‌های زیر را ویرایش کنید:

- `lib/data/courses.ts` - دوره‌ها
- `lib/data/teachers.ts` - اساتید
- `lib/data/students.ts` - دانشجویان
- `lib/data/services.ts` - خدمات
- `lib/data/posts.ts` - پست‌های وبلاگ
- `lib/data/testimonials.ts` - نظرات دانشجویان

### کامپوننت‌های جدید

- **CountUp**: انیمیشن شمارش از 0 به مقدار نهایی (برای آمار)
- **AnimatedSection**: کامپوننت wrapper برای scroll animations
- **TestimonialCard**: کارت نمایش نظرات دانشجویان
- **useScrollAnimation**: Hook برای تشخیص visibility و trigger کردن animations

### انیمیشن‌ها

انیمیشن‌های موجود در `app/globals.css`:
- `fade-in-up`: fade + slide up
- `fade-in`: فقط fade
- `slide-in-right`: slide از راست (RTL)
- `scale-in`: scale animation

استفاده:
```tsx
<AnimatedSection animation="fade-in-up" delay={100}>
  {/* محتوا */}
</AnimatedSection>
```

### تغییر رنگ‌ها

رنگ‌ها در `tailwind.config.ts` تعریف شده‌اند و می‌توانید آن‌ها را تغییر دهید.

### تغییر فونت

فونت در `app/globals.css` تنظیم شده است. می‌توانید فونت فارسی مورد نظر خود را اضافه کنید.

## پشتیبانی

برای سوالات یا مشکلات، می‌توانید با ما تماس بگیرید.

## کامپوننت‌های کلیدی

### CountUp
کامپوننت برای نمایش آمار با انیمیشن شمارش:
```tsx
<CountUp end={500} suffix="+" className="text-4xl font-bold" />
```

### AnimatedSection
کامپوننت wrapper برای scroll animations:
```tsx
<AnimatedSection animation="fade-in-up" delay={200}>
  <h2>عنوان</h2>
</AnimatedSection>
```

### TestimonialCard
کارت نمایش نظرات دانشجویان:
```tsx
<TestimonialCard testimonial={testimonial} />
```

## تصاویر

تصاویر باید در پوشه‌های زیر قرار گیرند:
- `public/images/logos/` - لوگوها
- `public/images/testimonials/` - تصاویر نظرات
- `public/images/courses/` - تصاویر دوره‌ها
- `public/images/services/` - تصاویر خدمات

## لایسنس

این پروژه برای استفاده شخصی است.

---

ساخته شده با ❤️ برای آکادمی 84

